export const TIME = {
  DEBOUNCE_DELAY: 1000,
  THIRTY_DAYS_IN_MS: 30 * 24 * 60 * 60 * 1000,
  REFRESH_INTERVAL: 5000,
}

export const VALIDATION = {
  MIN_TIMESTAMP: 0,
  MAX_TIMESTAMP: Date.now() + 365 * 24 * 60 * 60 * 1000,
  NETWORK_TIMEOUT_THRESHOLD: 5000,
  RENDER_THRESHOLD: 16,
  PERFORMANCE_SCORE_PENALTY: 1,
  NETWORK_ALERT_PENALTY: 5,
  PERFORMANCE_TIME_RANGES: [1000, 5000, 10000],
}

export const NETWORK = {
  DEFAULT_PORT: 5173,
  CHUNK_SIZE_WARNING_LIMIT: 1000,
}